# Base URL for the backend API used by the Expo app
# If you're running the backend locally, replace with your LAN IP so mobile devices can reach it.
EXPO_PUBLIC_API_BASE_URL=http://192.168.0.10:8080/api/v1
