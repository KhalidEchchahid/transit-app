{
  "name": "routing-benchmark",
  "version": "1.0.0",
  "description": "Benchmark for RAPTOR routing engines",
  "type": "module",
  "scripts": {
    "benchmark": "node benchmark.js",
    "benchmark:go": "node benchmark.js --backend go",
    "benchmark:nest": "node benchmark.js --backend nest",
    "benchmark:compare": "node benchmark.js --compare",
    "load-test": "node load-simulation.js",
    "load-test:light": "LOAD_LEVEL=light node load-simulation.js",
    "load-test:heavy": "LOAD_LEVEL=heavy node load-simulation.js"
  },
  "dependencies": {
    "autocannon": "^7.15.0",
    "chalk": "^5.3.0",
    "cli-table3": "^0.6.5"
  }
}
